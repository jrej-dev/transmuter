import { importTest } from "../utils/helpers";

// nft to token

describe("Transformer case 3", () => {
  // creator could either create a new token with max supply or send existing token to escrow
  // burn or transfer nft against allocation of token

  // importTest("Set up tests", `${__dirname}/1_init`);
  // importTest("Transmuter tests", `${__dirname}/2_transmuter`);
  // importTest("User tests", `${__dirname}/3_user`);
  // importTest("Updator tests", `${__dirname}/4_updator`);
  // importTest("Creator tests", `${__dirname}/5_creator`);
});
